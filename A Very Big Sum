#include <stdio.h>

int main(){
    int n;
    long int arr_i;
    scanf("%d",&n);
    long int sum = 0;

    long arr[n];
    for(arr_i = 0; arr_i < n; arr_i++){
       scanf("%li",&arr[arr_i]);
       sum += arr[arr_i];
    }

    printf("%li\n", sum);
    return 0;
}
